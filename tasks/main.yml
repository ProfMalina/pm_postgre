---
- name: Show info about the env this host belong to
  debug:
    msg: "This host is in {{ env }} environment!!!"

- block:
    - import_tasks: pre-install.yml
    - import_tasks: postgres-install.yml
  tags: install

- import_tasks: postgres-configure.yml
  tags: configure
